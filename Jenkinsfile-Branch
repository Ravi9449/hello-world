pipeline {
    agent any 
    stages{
        stage('Check'){
            steps{
                script {
                    //TAG_PREFIX = env.BRANCH_NAME | grep -oE 'PAP-[0-9]{4}'
                    TAG_PREFIX = env.BRANCH_NAME | grep -oE PAP-[0-9]{4} 
                    echo "${TAG_PREFIX}"
                }
            }
        }
    }
}