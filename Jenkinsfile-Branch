pipeline {
    agent any 
    stages{
        stage('Check'){
            steps{
                script {
                    TAG_PREFIX = git branch | grep -oE 'PAP-[0-9]{4}'
                    echo ${TAG_PREFIX}
                }
            }
        }
    }
}